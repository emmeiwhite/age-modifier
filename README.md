# Simple Redux Age Modifier in react-redux

## How to use:

- Download the zip, unzip, open project in terminal or command prompt.
- npm install   (to get node-module folder for all the dependencies the project is using)
- npm start
